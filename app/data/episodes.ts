import type { Episode } from '~/types'

/**
 * Original Hörverstehen material — written for this course, voiced with the
 * repo's own TTS pipeline, and therefore free to publish here.
 *
 * Nothing from a telc Übungstest belongs in this list. Those transcripts are
 * copyrighted; import that one file into the app instead (Eigene Dateien),
 * where it stays on the device.
 *
 * Add a file to public/audio/ and one entry here.
 */
export const EPISODES: Episode[] = []
