# pnpm blocks dependency build scripts unless they are allowed here.
# esbuild's postinstall fetches its platform binary — without it,
# `nuxt generate` fails locally and in CI alike.
allowBuilds:
  esbuild: true
